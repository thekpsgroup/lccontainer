---
// Exit-intent popup with local delivery guarantee and immediate callback
---

<script is:inline>
  let hasShownPopup = false;
  let mouseLeaveTimer;

  // Detect user's location for personalized messaging
  const referrer = document.referrer;
  let userCity = "";

  if (referrer.includes("/dallas/")) userCity = "Dallas";
  else if (referrer.includes("/houston/")) userCity = "Houston";
  else if (referrer.includes("/austin/")) userCity = "Austin";
  else if (referrer.includes("/fort-worth/")) userCity = "Fort Worth";
  else if (referrer.includes("/arlington/")) userCity = "Arlington";
  else if (referrer.includes("/plano/")) userCity = "Plano";

  function showExitIntentPopup() {
    if (hasShownPopup) return;

    hasShownPopup = true;
    const popup = document.getElementById("exit-intent-popup");
    const overlay = document.getElementById("popup-overlay");

    if (popup && overlay) {
      // Update city-specific messaging
      if (userCity) {
        const citySpans = popup.querySelectorAll(".popup-city");
        citySpans.forEach(function (span) {
          span.textContent = userCity;
        });

        const locationMessage = popup.querySelector(".location-message");
        if (locationMessage) {
          locationMessage.style.display = "block";
        }
      }

      overlay.style.display = "flex";

      // Animate in
      setTimeout(function () {
        popup.style.transform = "translateY(0)";
        popup.style.opacity = "1";
      }, 50);

      // Auto-hide after 30 seconds if no interaction
      setTimeout(function () {
        if (popup.style.opacity === "1") {
          hidePopup();
        }
      }, 30000);
    }
  }

  function hidePopup() {
    const popup = document.getElementById("exit-intent-popup");
    const overlay = document.getElementById("popup-overlay");

    if (popup && overlay) {
      popup.style.transform = "translateY(-50px)";
      popup.style.opacity = "0";

      setTimeout(function () {
        overlay.style.display = "none";
      }, 300);
    }
  }

  function trackConversion(type) {
    // Track conversion for analytics
    try {
      if (typeof window !== "undefined" && window.gtag) {
        window.gtag("event", "exit_intent_conversion", {
          conversion_type: type,
          user_city: userCity || "unknown",
          custom_map: { metric1: 1 },
        });
      }
    } catch (error) {
      console.debug("Analytics tracking failed:", error);
    }

    // You can also send to your analytics platform
    console.log("Exit intent conversion:", type, userCity);
  }

  document.addEventListener("DOMContentLoaded", function () {
    // Detect mouse leave from top of page (exit intent)
    document.addEventListener("mouseleave", function (e) {
      if (e.clientY <= 0 && !hasShownPopup) {
        clearTimeout(mouseLeaveTimer);
        mouseLeaveTimer = window.setTimeout(showExitIntentPopup, 500);
      }
    });

    // Cancel if mouse re-enters quickly
    document.addEventListener("mouseenter", function () {
      clearTimeout(mouseLeaveTimer);
    });

    // Show on mobile after scroll up gesture
    let lastScrollY = window.scrollY;
    let scrollUpCount = 0;

    window.addEventListener("scroll", function () {
      if (window.scrollY < lastScrollY) {
        scrollUpCount++;
        if (scrollUpCount > 3 && !hasShownPopup && window.scrollY > 500) {
          showExitIntentPopup();
        }
      } else {
        scrollUpCount = 0;
      }
      lastScrollY = window.scrollY;
    });

    // Time-based trigger (fallback after 45 seconds)
    setTimeout(() => {
      if (!hasShownPopup && window.scrollY > 1000) {
        showExitIntentPopup();
      }
    }, 45000);

    // Close popup handlers
    document
      .getElementById("close-popup")
      ?.addEventListener("click", hidePopup);
    document
      .getElementById("popup-overlay")
      ?.addEventListener("click", function (e) {
        if (e.target === this) hidePopup();
      });

    // CTA tracking
    document
      .getElementById("popup-phone-cta")
      ?.addEventListener("click", function () {
        trackConversion("phone_call");
      });

    document
      .getElementById("popup-callback-cta")
      ?.addEventListener("click", function () {
        trackConversion("callback_request");

        // Update button state
        this.innerHTML = "✅ We'll call you back in 10 minutes!";
        this.disabled = true;
        this.classList.add("bg-green-600", "hover:bg-green-600");
      });

    // Escape key to close
    document.addEventListener("keydown", function (e) {
      if (e.key === "Escape") hidePopup();
    });
  });
</script>

<!-- Exit Intent Popup Overlay -->
<div
  id="popup-overlay"
  class="fixed inset-0 bg-black/50 backdrop-blur-sm z-[9999] flex items-center justify-center p-4"
  style="display: none;"
>
  <div
    id="exit-intent-popup"
    class="bg-white dark:bg-gray-800 rounded-2xl shadow-2xl max-w-md w-full p-8 relative transform -translate-y-12 opacity-0 transition-all duration-300"
  >
    <!-- Close button -->
    <button
      id="close-popup"
      class="absolute top-4 right-4 text-gray-400 hover:text-gray-600 dark:hover:text-gray-300 transition-colors"
      aria-label="Close popup"
    >
      <svg
        class="w-6 h-6"
        fill="none"
        stroke="currentColor"
        viewBox="0 0 24 24"
      >
        <path
          stroke-linecap="round"
          stroke-linejoin="round"
          stroke-width="2"
          d="M6 18L18 6M6 6l12 12"></path>
      </svg>
    </button>

    <!-- Urgent headline -->
    <div class="text-center mb-6">
      <div class="mb-4">
        <div
          class="w-16 h-16 bg-gradient-to-r from-brand-red to-red-600 rounded-full flex items-center justify-center mx-auto mb-4 animate-pulse"
        >
          <svg
            class="w-8 h-8 text-white"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
        </div>
      </div>

      <h3 class="text-2xl font-bold text-gray-900 dark:text-white mb-2">
        Wait! Don't Leave Empty-Handed
      </h3>

      <p class="text-gray-600 dark:text-gray-300 text-lg">
        Get your <strong class="text-brand-red">FREE quote</strong> in the next 2
        hours
      </p>
    </div>

    <!-- Location-specific message (hidden by default) -->
    <div
      class="location-message hidden bg-blue-50 dark:bg-blue-900/30 border border-blue-200 dark:border-blue-700 rounded-lg p-4 mb-6"
    >
      <div class="flex items-center gap-3">
        <svg
          class="w-5 h-5 text-blue-600"
          fill="none"
          stroke="currentColor"
          viewBox="0 0 24 24"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"
          ></path>
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
        </svg>
        <div>
          <div class="font-semibold text-blue-800 dark:text-blue-300">
            🚚 Same-day delivery to <span class="popup-city"></span>!
          </div>
          <div class="text-sm text-blue-600 dark:text-blue-400">
            Limited delivery slots remaining today
          </div>
        </div>
      </div>
    </div>

    <!-- Offer details -->
    <div class="space-y-3 mb-6">
      <div class="flex items-center gap-3">
        <div
          class="w-8 h-8 bg-green-100 dark:bg-green-900/30 rounded-full flex items-center justify-center"
        >
          <svg
            class="w-4 h-4 text-green-600"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M5 13l4 4L19 7"></path>
          </svg>
        </div>
        <span class="text-gray-700 dark:text-gray-300"
          ><strong>Free</strong> no-obligation quote</span
        >
      </div>

      <div class="flex items-center gap-3">
        <div
          class="w-8 h-8 bg-green-100 dark:bg-green-900/30 rounded-full flex items-center justify-center"
        >
          <svg
            class="w-4 h-4 text-green-600"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
        </div>
        <span class="text-gray-700 dark:text-gray-300"
          >Response in <strong>2 hours guaranteed</strong></span
        >
      </div>

      <div class="flex items-center gap-3">
        <div
          class="w-8 h-8 bg-green-100 dark:bg-green-900/30 rounded-full flex items-center justify-center"
        >
          <svg
            class="w-4 h-4 text-green-600"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M13 10V3L4 14h7v7l9-11h-7z"></path>
          </svg>
        </div>
        <span class="text-gray-700 dark:text-gray-300"
          >Same-day delivery available</span
        >
      </div>
    </div>

    <!-- CTA Buttons -->
    <div class="space-y-3">
      <a
        id="popup-phone-cta"
        href="tel:12145244168"
        class="w-full bg-gradient-to-r from-brand-red to-red-600 text-white text-center py-4 px-6 rounded-xl font-bold text-lg hover:shadow-lg transition-all duration-200 flex items-center justify-center gap-3 group"
      >
        <svg
          class="w-6 h-6 group-hover:animate-pulse"
          fill="none"
          stroke="currentColor"
          viewBox="0 0 24 24"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"
          ></path>
        </svg>
        Call Now: (214) 524-4168
      </a>

      <button
        id="popup-callback-cta"
        class="w-full bg-blue-600 hover:bg-blue-700 text-white text-center py-3 px-6 rounded-xl font-semibold transition-all duration-200 flex items-center justify-center gap-3"
      >
        <svg
          class="w-5 h-5"
          fill="none"
          stroke="currentColor"
          viewBox="0 0 24 24"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"
          ></path>
        </svg>
        Request Immediate Callback
      </button>
    </div>

    <!-- Trust indicator -->
    <div class="mt-6 text-center text-sm text-gray-500 dark:text-gray-400">
      <div class="flex items-center justify-center gap-2 mb-2">
        <svg
          class="w-4 h-4 text-yellow-500"
          fill="currentColor"
          viewBox="0 0 20 20"
        >
          <path
            d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"
          ></path>
        </svg>
        <span class="font-semibold">4.9/5 rating</span>
        <span>• 500+ happy customers</span>
      </div>
      <div>🔒 Licensed & Insured • BBB A+ Rating • 20+ Years Experience</div>
    </div>
  </div>
</div>
